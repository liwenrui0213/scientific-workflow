interface:
  display_name: "Bootstrap Scientific Workflow"
  short_description: "Adapt and install the workflow in a science repository"
  default_prompt: "Use $bootstrap-scientific-workflow to adapt and install this scientific workflow in the current repository."
policy:
  allow_implicit_invocation: false
